html
  head
    title #{title}
    each val in styles
      link(href=val,rel="stylesheet")
    each val in scripts
      script(src=val)
    block scripts
    block stylesheets
    script(type='text/javascript').
      window.__INITIAL_STATE__ = !{JSON.stringify(data)}
  body
    block content
